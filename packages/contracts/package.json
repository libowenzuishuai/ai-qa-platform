{
  "name": "@ai-qa/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "领域契约：状态机、枚举、TestPlan v1、API 错误结构（Zod 运行时校验）",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "zod": "^3.25.0"
  }
}
