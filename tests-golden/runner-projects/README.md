# 自有运行器公开下载夹具

仅供运行器端到端评测，不是客户项目或模型输入。四个互不依赖的目录分别验证 Node/Python 的通过与故意失败。

先将此目录上传到开发分支；测试通过完整提交 SHA 从 GitHub 下载该提交，然后让真正的运行器 CLI 领取任务、在 Docker 内执行、回传结构化结果。使用本地 `source_directory` 不能算此项验收通过。

目录名中的 fail 是评测器标签，不输入智能规划或验收生成流程。
