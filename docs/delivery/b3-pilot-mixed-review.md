# B3 试点与混排评测复核

2026-09-21：纳入 `v1/b3-pilot-and-mixed@e81cc3c`（两个提交）。b3-08 的试点观察记录与 mock 清单可以接收；不是官方 PRD 验收完成。

原 b3-09 real 文件保留：包含三个 Kimi 调用的元数据和合并文本，12 个去重关键词命中。该记录证明的范围不包含逐页位置、表格关系或人工复核；不回填或重写为更强的验收结果。

修复新脚本：

- 14 项逐页文本检查、4 项角色与额度同行关系检查，检查三页完整覆盖；关键词在别页出现不能借用。
- 明确 `manualReviewPerformed=false`。自动比对不命名为人工已验收。
- 每次输出独立文件；已存在的目标在调用模型前即拒绝覆盖。
- 解析/模型异常和断言失败也保存状态、校验和、已发生调用；不为通过而放宽标准。

默认验证 `pnpm test:doc-ingestion`：64 项通过。包含页码调换、角色额度互换、模型超时留档与防覆盖反例。随后按已有授权单独重跑真实 Kimi：14/14 逐页文本与 4/4 角色额度关系检查通过，3 次模型调用。见 [新评测记录](../../services/intelligence/tests/doc_ingestion/fixtures/b3-records/runs/b3-mixed-20260921T063847-6831cc09.real.json)。首次尝试因本机代理依赖缺失，在调用前失败；失败记录同目录保留。以上是自动验收，没有冒充人工逐项复核。
