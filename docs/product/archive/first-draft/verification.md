# 产品 1.0 文稿与原型检查记录

日期：2026-09-20。设计依据：main `8491c4c`；没有把原型功能写成已实现能力。

## 文档检查

- 检查旧 PRD、产品路线、实施状态、平台工作台说明及 `apps/web` 实际源文件。
- 原有登录/项目/运行/报告/资料审阅页面可运行；未发现独立静态或设计工具形式的完整产品原型。
- 纠正 `apps/web/README.md` 的“Next.js 占位、无代码”说明。
- 新增 1.0 PRD：两个接入入口、健康/业务两种模式、12 组功能要求、版本范围、状态与指标、数据边界、验收和三人交付顺序。
- 明确与旧 0.1 范围的差异、已有/部分/未实现能力、真实模型小样不等于全面质量评测。

## Chromium 交互检查

通过 **50 项检查**。桌面尺寸 1440×1050，窄屏尺寸 390×844。

- 9 个页面都能打开；桌面和窄屏无整体横向溢出，宽表格在自身区域滚动。
- 顶部始终显示交互原型/示例标记。
- Git 地址含多余参数时拒绝；端口越界时拒绝；上一步保留已编辑命令。
- Git 接入完成进入健康检查；健康报告不冒充业务验收。
- 澄清缺内容/来源不能保存；未解决不能批准；批准后确认记录只读。
- 在输入中放入 HTML/事件代码仅显示为文本，不创建元素、不执行脚本。
- 用例搜索、关联规则批准后用例草稿审批、观察绑定能正确更新本地演示状态。
- 模拟运行结束后可查看报告；取消后进度停止，取消报告显示未完成。
- 业务报告正确展示 2/4=50% 通过比例、3/4=75% 执行率。
- 页面证据明确注明示意；弹层可用 Esc 关闭。
- 复测记录关联新构建，原失败保留；导出 JSON 含 `prototype: true` 与示例声明。
- 浏览器脚本错误：0；外部网络请求：0。

初次自动检查在 hash 导航完成前读取新标题导致提前判断；检查脚本补齐导航等待后通过，没有放宽页面结果断言。

## 视觉检查

已查看项目概览、接入配置、缺陷报告与窄屏概览截图，检查文字、对齐、卡片、表格与按钮。最终预览保存在 `prototype/previews/`；这些是设计页面截图，不是测试执行证据。

## 未做的事情

本次不调用付费模型、不读取密钥、不运行待测仓库、不迁移数据库；原型是独立静态文件，未替换现有平台页面。本次验证仅证明文稿/原型的可读性与交互，不证明 PRD 中的目标后端功能已交付。

本地预览只服务 `docs/product/v1.0`，默认地址 `http://127.0.0.1:7840/prototype/`。停止本地服务后仍可直接打开 `prototype/index.html`。
