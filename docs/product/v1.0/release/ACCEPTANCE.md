# 1.0 发布验收矩阵

2026-09-21｜设计基线 b559d3f｜配套 [PRD](../PRD.md)、[GLM 任务书](../GLM-COMPLETE-IMPLEMENTATION.md)。

**这是剩余工作的验收清单，不是通过记录。** 初始状态反映现有代码与本轮文档核查；执行人按真实证据更新。历史回归不可直接填到新增功能。

## 1. 状态与证据口径

代码状态：TODO / IN_PROGRESS / CODE_VERIFIED。发布门：NOT_RUN / PASS / FAIL / EXTERNAL_PENDING。每门独立记录，不将 EXTERNAL_PENDING、跳过、模拟检查计为真实发布 PASS。

每条通过记录最少含：commit、实际日期、测试入口/命令、执行环境、样本/数量、证据文件/校验和、组件真实性、剩余限制。复用历史证据必须标出其 commit 和未变化的适用范围；无依据不能打勾。

## 2. 工作包门槛

| 编号 | PRD 对应 | 本轮起始状态 | 正向验收 | 关键拒绝/故障验收 | 所需证据 |
|---|---|---|---|---|---|
| R00 | §11、FR-CTX-03/04 | TODO；现有单文件契约已具备 | 新增契约单源生成、TS/Python 共用向量通过 | 路径穿越、重复路径、跨版本、输出覆盖缺漏、类型与语义不一致拒绝 | schema/check 日志，共享向量及双端测试路径 |
| R01 | FR-CTX-03 | TODO；单文件模块可复用 | 多文件增删改/纯重命名/无变化正确，稳定输出 | 不完整扫描不判删除；重复哈希不强配对；质量降低不忽略 | 可移植旧/新目录、manifest/hash、预期结果、单元反例 |
| R02 | FR-CTX-03、FR-DESIGN-02 | TODO；单文件平台闭环已具备 | 实际快照→分析→复核→新增/替换资产→新版基线 | 队列失败、SIGKILL、重复投递、越权、漏新增、旧标准漂移 | 真 DB/HTTP/Python/浏览器集成，冻结前后对比 |
| R03 | FR-CTX-04 | TODO | 长资料所有片段有处理/上下文/阻塞归属，跨块结果合并、恢复 | 尾块失败/截断不能完整成功；中文 emoji 偏移；跨块冲突；预算耗尽 | 覆盖报告、故障恢复记录、mock/real 分列 |
| R04 | FR-OPS-03 | TODO；当前 Compose 非完整发布验证 | 干净安装、全服务就绪、备份恢复、保留数据升级 | 缺配置/迁移失败/依赖未就绪准确失败；镜像不依赖宿主 node_modules | OS/架构、镜像 digest、隔离安装/升级日志、恢复后对象核对 |
| R05 | FR-EXEC-03/04 | TODO；基础 Node/pytest runner 已具备 | 新测试框架结果导入、代码健康单列、Node 模板部署及清理 | 零测试、坏报告、错误 SHA、恶意脚本、越界、过期租约、清理失败 | 真隔离运行器/浏览器/API与资源清理记录 |
| R06 | FR-CTX-01、FR-OPS-01/02 | TODO；真实 GitHub App 配置待提供 | 私库限定范围同步、CI 固定 SHA 触发/Check 回传 | 伪签名、重放、撤销、错误 repo/project、fork 泄密拒绝 | 真实 HTTP 协议测试 + 独立真实 GitHub 验证 |
| R07 | FR-AGENT-01 | TODO；固定工作流已具备 | UI 创建/发布/运行三种 DAG 模板，固定版本与恢复 | 循环、悬空、类型不符、越权、缺清理、写入重复、模板漂移拒绝 | 模板版本、节点事件、实际工具输出、SIGKILL、页面截图 |
| R08 | FR-AGENT-02 | TODO | 自然语言形成有依据草案，批准后执行；观察/诊断可追溯 | 未知工具、过期/跨项目记忆、注入、预算越界、不准擅改预期 | 协议反例、实际模型单列、工具调用与记忆失效记录 |
| R09 | FR-RESULT、FR-OPS-02 | TODO；结果聚合/复测已具备 | 指派、决策、导出、完整统计、保留策略 | 接受风险不改 FAIL；缺证据不可信 PASS；越权导出拒绝 | 同一报告 UI/API/导出核对、审计与旧记录保留 |
| R10 | §10 | IN_PROGRESS（现有页面可复用，剩余新页未接） | 所有核心旅程真实表单可用、分页和统计完整、桌面/手机 | 空/错误/未配置/无权限、重复提交、断线恢复、溢出 | 1440/390 截图及人工查看记录、浏览器流程测试 |
| R11 | §13 | EXTERNAL_PENDING（仅发布门；代码夹具可继续） | 两种实际项目，两类入口，批准完整 PRD，健康/缺陷/修复对照 | 缺账号/依据必须阻塞，ground truth 隔离，保留模型首败 | 脱敏资料 manifest、审批、授权范围、真实 Run/模型记录 |
| R12 | §13、全 PRD | TODO | 独立 review、最终回归、最终镜像验证、文档与远端一致 | 不漏未完成/跳过/外部条件，不提交秘密，不绕过保护分支 | 最终 commit、审阅发现修复表、发布状态、remote SHA |

R11 行的 EXTERNAL_PENDING 是发布状态；其评测导入/脚本等代码状态初始仍为 TODO。后续填写应使用独立代码状态/发布状态两列，不能混为单一“完成百分比”。

## 3. 横向不可退让门

| 门 | 通过条件 | 当前新增范围状态 |
|---|---|---|
| G01 真实性 | 所有已发布结果能关联实际组件与证据，不使用占位节点 | NOT_RUN |
| G02 独立依据 | 原文/引用/批准/版本冻结有效，模型无法改判 | NOT_RUN |
| G03 故障恢复 | 入队失败、取消、超时、SIGKILL、过期租约收敛；未知写入不重放 | NOT_RUN |
| G04 权限与隔离 | 新入口跨项目反例拒绝，凭据/原始证据无越权路径 | NOT_RUN |
| G05 完整性 | 多文件/长文/列表/报告无静默截断，分母和未验证范围明确 | NOT_RUN |
| G06 部署 | 最终提交可干净安装、升级、恢复；不依赖开发宿主依赖 | NOT_RUN |
| G07 真实试点 | 两个实际业务类型、完整资料与批准、原标准复测 | EXTERNAL_PENDING |
| G08 模型评测 | 实际模型/输入/用量/首败/重试留档，核心语义核验 | NOT_RUN（已有 B3 仅覆盖历史混排子项） |
| G09 可用性 | 核心业务操作无需内部 ID/JSON，真实浏览器与两种宽度验收 | NOT_RUN |
| G10 发布一致性 | 代码、迁移、生成契约、文档、镜像与远端版本相互对应 | NOT_RUN |

## 4. 真实试点资料清单与当前可用项

| 输入 | 当前情况 | 交付方式 |
|---|---|---|
| Dify 脱敏观察 | 已有；非官方 PRD | `packages/contracts/fixtures/pilot-dify-v1` |
| Dify 候选设计 | 已有；未经业务正式批准 | `docs/delivery/pilot-dify-candidate-design.md` |
| 多文件/长文合成夹具 | 需构造 | 独立旧/新文件、hash、expected、synthetic 标记 |
| 完整业务 PRD 与权威性确认 | 未提供 | 版本化文档、来源/许可说明、人工审核记录 |
| 实际角色/权限矩阵与账号引用 | 未齐 | 环境登记 + 安全凭据引用，不放 Markdown 密码 |
| 第二实际业务项目 | 未提供，无确定日期 | 项目/仓库/网址、资料位置、允许的操作范围 |
| GitHub App 真实安装配置 | 未核实可用 | 受支持配置与仓库限定授权；缺失则外部待验 |
| 健康/隐蔽缺陷/修复版本 | 完整真实项目未齐 | 冻结提交/构建、独立评测 ground truth、执行输入分离 |

## 5. 发布决策规则

1. R00–R10 及 R12 的可独立代码、组件验收不得因为 R11 缺资料而停工。
2. 所有必做代码状态 CODE_VERIFIED 后，才能声明本轮代码验收完成；仍有已知行为缺陷不得标完成。
3. 所有横向门 PASS，真实试点通过，方可声明 1.0 发布验收通过。EXTERNAL_PENDING 只能说明原因与补充条件，不能豁免为 PASS。
4. 同一新问题修复后跑其反例与相关回归。文档/样式小修改不机械跑全部历史收费评测。
5. 每个子项的 N/A 需指出明确产品范围及审阅依据；不能将必做功能临时改为 N/A 以结项。

## 6. 交付记录模板

| R/门编号 | 代码状态 | 发布状态 | commit | 实际命令/环境/日期 | 结果与组件真实性 | 证据 | 限制/下一动作 |
|---|---|---|---|---|---|---|---|
| 待实施后填写 | TODO | NOT_RUN | — | — | 未运行 | — | 按任务书实施 |
