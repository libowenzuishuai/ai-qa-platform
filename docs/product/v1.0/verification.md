# 产品重设计 r2 · 检查记录

2026-09-20。实现基线：main `8491c4c`。本次只修改产品文稿、静态原型和文档入口，不修改后端、数据库或运行器。

## 交付

- `PRODUCT-STRATEGY.md`：定位、首批客户假设、市场参考、差异化、商业形态、试点与度量。
- `PRD.md`：统一接入、来源与业务依据、目标驱动任务、执行能力、验收判定、治理、领域模型和分步发布门槛。
- `prototype/`：七页独立原型，桌面 / 手机布局；旧导航入口兼容。
- 第一版 PRD、原型与检查记录归档到 `../archive/first-draft/`。

## Chromium 验证

**52 项通过，浏览器脚本错误 0，外部请求 0。** 逐项结果保存在 [browser-checks.json](browser-checks.json)。使用 1440×1050 和 390×844 视口。

验证覆盖：

- 七页导航、旧 runs 路由、桌面和手机整体宽度。
- 常驻示例标识；仓库发现明确固定样例，没有暗示读取了输入地址。
- 仓库地址校验、空资料集合拒绝、统一资料纳入。
- 业务冲突阻止开始、确认必须填写理由；用户输入 HTML 仅作为文本展示。
- 完整执行、取消停止、取消后的 22 项未执行状态。
- 默认结果 22 项：19 通过、1 失败、1 阻塞、1 未执行；86.4% / 90.9% 两个指标。
- 关闭 API 能力后，其 4 项进入未执行；22 项分母保留，通过率为 72.7%，整体不完整。
- 交付决定必须说明理由；接受风险不会把 FAIL 改为 PASS。
- JSON 导出包含 prototype / realExecution 标识、原始计数和交付决定。
- 资料变更不改写已运行快照；复测草稿保留原失败；体检通过不代表业务验收。
- Harness 节点说明、弹窗 Esc 关闭、无脚本错误和外部请求。

第二次检查脚本曾选到弹窗后方同名按钮，造成等待超时；将操作限定到当前弹窗后重新通过，未放宽产品断言。

## 视觉检查

检查工作台、资料发现、上下文、测试方案、交付评估与手机布局。预览在 `prototype/previews/`。截图是设计评审材料，不是业务系统执行证据。

## 限制

- 没有调用 GitHub / 模型 / 真实项目，没有使用密钥或付费服务。
- 运行、生成和证据均为本地示例；当前能力、1.0 目标和未来扩展分别标识。
- 新复测只生成目标草稿；没有完成新构建选择、历史持久化和真实复测。
- 商业假设尚未经过用户访谈或付费验证，官方竞争产品资料不构成独立性能评测。
- 原型测试不等于后端功能验收。本次没有运行无关的数据库 / 执行器回归。
